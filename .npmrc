; Aplica --use-system-ca aos scripts npm (dev/build/start) para confiar na CA do sistema.
; Necessário em redes com proxy/inspeção SSL (ex.: UNABLE_TO_VERIFY_LEAF_SIGNATURE).
; Requer Node 22.15+ (no Node 20 a flag --use-system-ca nao existe e quebra os scripts).
node-options=--use-system-ca
